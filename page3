<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { 
            background-color: black; 
            color: white; 
            font-family: 'Segoe UI', Arial, sans-serif; 
            margin: 0; 
            padding: 40px; 
            display: flex; 
            flex-direction: column;
            align-items: center; 
        }
        .container { display: flex; gap: 60px; text-align: center; margin-bottom: 40px; }
        
        .day-header { margin-bottom: 20px; }
        
        .day-label { 
            font-size: 1.6em; 
            color: white; 
            font-weight: bold;
            text-transform: uppercase;
            margin: 0;
        }

        .day-date {
            font-size: 1.1em;
            color: #888;
            margin-top: 4px;
        }

        .map-container img { 
            width: 220px; 
            height: auto; 
            border-radius: 4px;
            margin-bottom: 20px;
        }

        .rating { 
            font-size: 2em; 
            font-weight: 900; 
            text-transform: uppercase; 
            letter-spacing: 1px;
        }

        /* Footer Line */
        .footer {
            border-top: 1px solid #333;
            width: 80%;
            padding-top: 15px;
            color: #555;
            font-size: 0.85em;
            text-align: center;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        /* CFA Official Color Palette */
        .MODERATE { color: #78be20; } 
        .HIGH { color: #ffb81c; } 
        .EXTREME { color: #e03c31; } 
        .CATASTROPHIC { color: white; background: red; padding: 0
